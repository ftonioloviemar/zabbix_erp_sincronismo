# =============================================================================
# CONFIGURAÇÕES DO MONITORAMENTO DE SINCRONISMO ERP
# =============================================================================

# URL base do sistema ERP (obrigatório)
# Exemplos: "http://erpdireto:8080", "https://erp.empresa.com:8443"
ERP_BASE_URL="http://erpdireto:8080"

# Nome de usuário para monitoramento (obrigatório)
# Este usuário deve ter permissão para acessar a tela de sincronismo
ERP_USERNAME="MONITORSINCRONISMO"

# NOTA: A senha NÃO deve ser configurada aqui.
# Use o script encrypt_password.py para criptografar a senha:
#   uv run encrypt_password.py
# Isso criará os arquivos .key e .bin com a senha criptografada

# =============================================================================
# PARÂMETROS DE MONITORAMENTO
# =============================================================================

# Limite de tempo em segundos para considerar o sincronismo atrasado (opcional)
# Valor padrão: 300 (5 minutos)
# Aumente este valor se o sincronismo normalmente demora mais
MAX_SECONDS_DELAY=300

# =============================================================================
# CONFIGURAÇÕES AVANÇADAS (NÃO ALTERAR SEM CONHECIMENTO)
# =============================================================================

# Habilitar modo debug (True/False) - não recomendado em produção
# DEBUG_MODE=False

# Timeout para requisições HTTP em segundos (opcional)
# HTTP_TIMEOUT=30

# Número de tentativas de login em caso de falha (opcional)
# LOGIN_RETRIES=3
